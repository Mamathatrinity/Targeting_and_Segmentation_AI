# Screenshots

This directory contains screenshots captured during test execution.

Screenshots are automatically taken when:
- A test fails (configured in conftest.py)
- Manually triggered using `take_screenshot()` helper

Files are named: `{test_name}_{timestamp}.png`
